# Copy to .env for local (non-Docker) dev — Expo loads EXPO_PUBLIC_* vars from this automatically.
# The Dockerized build (frontend/Dockerfile) sets this via a build arg instead, see
# devops/frontend/docker-compose.yml.
EXPO_PUBLIC_GATEWAY_ORIGIN=http://localhost:8000
