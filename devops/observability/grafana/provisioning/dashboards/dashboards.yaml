apiVersion: 1

providers:
  - name: shabbat-notifier
    orgId: 1
    folder: shabbat-notifier
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
