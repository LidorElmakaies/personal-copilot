# Read by `docker compose -f docker-compose.cloud.yml` from devops/frontend/ — copy to
# .env.cloud. Cloud/Hetzner deployment only. See docs/frontend/environment.md.

DOMAIN=example.com
GATEWAY_TUNNEL_PORT=8000
